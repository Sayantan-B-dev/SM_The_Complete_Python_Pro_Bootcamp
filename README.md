# root files
- > `.gitattributes`, its the git attributes file, used for line endings.
- > `.gitignore`, its the git ignore file, used for ignore files.
- > `README.md`, its the readme file, used for project description.
- > ignore `bulk_rename.py`, its used for rename files in the project.
- > use `create_course_tree.sh` to create a tree of the course from `output.txt`.
- > use `extract_data.py` to sanitize the html content from `input.txt` and create `output.txt`.
- > ignore `full_tree.txt`, its the entire project tree.
- > copy html into `input.txt`.
- > ignore `output.txt`, its the output of `extract_data.py`.
- > ignore `tree.py`, its used for get the entire project tree.
- > use `filecount.py` to count the number of files in the project.
- > use `stat.py` to get the file and their total consumed storage stats.

```
`index.html` -> 
`extract_data.py` -> 
`output.html ` -> 
`create_course_tree.sh ` -> 
`bulk_rename.py `
```
---

# UNDONE
- > Section 50 is pending
- > Section 51 is pending
- > Section 52 is pending
- > Section 53 is pending
- > Section 93 is pending